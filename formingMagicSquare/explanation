This program finds the lowest cost to turn a 3x3 grid of numbers into a magic square, where each row, column, 
and diagonal adds up to 15. It checks the input against all 8 possible magic square combinations, calculates 
how much each number needs to change, and returns the smallest total cost.
