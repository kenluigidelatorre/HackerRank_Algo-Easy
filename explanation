This program performs one step of the insertion sort algorithm on a list of numbers. It takes the last element and inserts 
it into its correct position by shifting larger elements to the right, printing the array after each shift and after placing 
the element in its final spot.
