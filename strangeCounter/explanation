This program simulates a strange countdown timer. The timer starts at 3 and counts down by 1 every second. 
When it reaches 1, instead of stopping, it resets to double its previous starting value (e.g., 3 → 6 → 12 → 24, etc.) 
and continues counting down in the same pattern.
